# backend/plan_extractor/placement/__init__.py
# Phase 3a — Automated Equipment Placement (fire extinguishers, dots only).
#
# Reads wall/room vector geometry (never touches the rules engine files
# directly — only imports class_a_checker.CLASS_A_TABLE, read-only).
